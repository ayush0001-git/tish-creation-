# Tish Creations — robots.txt
# Allow all crawlers full access. Sitemap is declared below.

User-agent: *
Allow: /

# Block the admin entry (it's a UI feature, not content to index)
# L2 fix: removed "Disallow: /#admin" — crawlers ignore URL fragments, so it
# was a no-op. The ?admin=1 query param version below does work.
Disallow: /*?admin=1

# Sitemap (update the domain to your real production domain)
Sitemap: https://www.tishcreations.com/sitemap.xml
